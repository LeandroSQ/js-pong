import { Main } from "./../main";
import { StatePlay } from "../states/state-play";

export class Agent {

	constructor(private main: Main, private state: StatePlay) { }

	update(deltaTime: number) {

	}

}